# Sun Speech Notes

This is a deliberately small demonstration language pack.

Its purpose is to prove that the backend can load a different lexicon and rules set without requiring language-specific backend code.

Sample lines:

- `Mir ka` -> `The sun rose.`
- `Lum dor` -> `The stone glowed.`
